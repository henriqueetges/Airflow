name: Check Dags and Models

on:
  push:
    branches: 
      - main

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  airflow_dbt_checks:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set Python
        uses: actions/setup-python@v4
        with:
          python-version=: '3.9'
      
      - name: Clear cache
        run: |
          pip cache purge

      - name: install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install dbt_core dbt_postgres 
          pip install astronomer-providers 
          pip install apache-airflow-providers-postgres==6.0.0rc2
          pip install sqlalchemy
      
      - name: Show packages
        run: |
          pip show apache-airflow
          pip show apache-airflow-providers-postgres
      
      - name: Validate DAGs
        run: |
          python .github/validate_dags.py
      
      - name: Run dbt Debug
        run: |
          cd dags/carteira_dbt_airflow
          dbt debug          
      
